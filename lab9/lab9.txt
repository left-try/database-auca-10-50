postgres=# CREATE TABLE Students (
    student_id SERIAL PRIMARY KEY,
    first_name VARCHAR(50),
    last_name VARCHAR(50),
    birth_date DATE
);
ERROR:  relation "students" already exists
postgres=# drop table students cascade;
DROP TABLE
postgres=# CREATE TABLE Students (
    student_id SERIAL PRIMARY KEY,
    first_name VARCHAR(50),
    last_name VARCHAR(50),
    birth_date DATE
);
CREATE TABLE
postgres=# INSERT INTO Students (first_name, last_name, birth_date) VALUES ('Aibek', 'Sharshenov', '2002-05-12');
INSERT 0 1
postgres=# INSERT INTO Students (first_name, last_name, birth_date) VALUES
postgres-# ('Ainura', 'Toktomamatova', '2001-09-23'),
('Bakyt', 'Mamatov', '2003-01-15'),
('Gulzat', 'Sultanova', '2002-07-30');
INSERT 0 3
postgres=# UPDATE Students SET birth_date = '2002-05-15' WHERE first_name = 'Aibek' AND last_name = 'Sharshenov';
UPDATE 1
postgres=# DELETE FROM Students
WHERE first_name = 'Gulzat' AND last_name = 'Sultanova';
DELETE 1
postgres=# DELETE FROM Students
WHERE birth_date < '2002-01-01';
DELETE 1
postgres=# COPY Students(first_name, last_name, birth_date)
FROM '/home/fum/students.csv'
DELIMITER ','
CSV HEADER;
postgres=# UPDATE Students
SET last_name = 'Bekov'
WHERE last_name IN ('Uulu', 'Isakov');
UPDATE 0
postgres=# DELETE FROM Students
WHERE student_id IN (2, 3, 5);
DELETE 1
postgres=#